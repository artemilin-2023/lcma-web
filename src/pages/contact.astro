---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import { SOCIAL_LINKS } from '../consts';
---

<Layout title="Контакты | lcma.tech" description="Свяжитесь со мной через социальные сети">
	<section class="contact-hero">
		<h1 class="neon-text-flicker">Связь со мной</h1>
		<p class="subtitle">Я всегда рад новым знакомствам и сотрудничеству!</p>
	</section>

	<section class="contact-content">
		<div class="contact-container">
			<div class="contact-info">
				<div class="contact-card neon-box">
					<h2 class="gradient-text">Способы связи</h2>
					
					<div class="contact-details">
						<div class="contact-item">
							<div class="contact-icon">✉️</div>
							<div class="contact-text">
								<h3>Email</h3>
								<p><a href={SOCIAL_LINKS.mail}>{SOCIAL_LINKS.mail.replace('mailto:', '')}</a></p>
							</div>
						</div>
						
						<div class="contact-item">
							<div class="contact-icon">🌐</div>
							<div class="contact-text">
								<h3>Социальные сети</h3>
								<div class="social-links">
									<a href={SOCIAL_LINKS.telegram} class="social-link">Telegram</a>
									<a href={SOCIAL_LINKS.github} class="social-link">GitHub</a>
									<a href={SOCIAL_LINKS.hh} class="social-link">HeadHunter</a>
									<a href={SOCIAL_LINKS.spotify} class="social-link">Spotify</a>
								</div>
							</div>
						</div>
					</div>
				</div>
				
				<div class="kawaii-message neon-box">
					<div class="kawaii-avatar">
						<Image src="/images/placeholder-avatar.png" alt="Аватар" width={150} height={150} loading="eager" />
					</div>
					<div class="message-bubble">
						<p>Буду рад общению с вами! Давайте обсудим аниме, веб-разработку или любые другие интересующие вас темы 💜</p>
					</div>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	.contact-hero {
		text-align: center;
		padding: 3rem 0;
	}
	
	.contact-hero h1 {
		font-family: var(--font-heading);
		font-size: 3rem;
		margin-bottom: 1rem;
	}
	
	.subtitle {
		font-family: var(--font-kawaii);
		font-size: 1.2rem;
		opacity: 0.9;
	}
	
	.contact-content {
		margin-bottom: 4rem;
	}
	
	.contact-container {
		display: flex;
		justify-content: center;
	}
	
	.contact-info {
		max-width: 600px;
		width: 100%;
	}
	
	.contact-card {
		padding: 2rem;
		margin-bottom: 2rem;
	}
	
	.contact-card h2 {
		font-family: var(--font-heading);
		margin-bottom: 1.5rem;
		text-align: center;
	}
	
	.contact-details {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	
	.contact-item {
		display: flex;
		align-items: flex-start;
		gap: 1rem;
		min-height: 80px;
	}
	
	.contact-icon {
		font-size: 1.8rem;
		line-height: 1;
		display: flex;
		align-items: flex-start;
		justify-content: center;
		width: 40px;
		padding-top: 5px;
		min-height: 40px;
	}
	
	.contact-text {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		justify-content: flex-start;
	}
	
	.contact-text h3 {
		font-family: var(--font-kawaii);
		margin: 0;
		padding: 0;
		color: var(--neon-pink);
	}
	
	.social-links {
		display: flex;
		gap: 1rem;
	}
	
	.social-link {
		font-family: var(--font-accent);
		display: inline-block;
		padding: 0.25rem 0.75rem;
		border-radius: 3rem;
		font-size: 0.9rem;
		background: rgba(138, 43, 226, 0.2);
		transition: all 0.3s ease;
	}
	
	.social-link:hover {
		background: rgba(138, 43, 226, 0.4);
		box-shadow: 0 0 5px var(--neon-purple);
	}
	
	.kawaii-message {
		padding: 1.5rem;
		display: flex;
		gap: 1rem;
		align-items: center;
	}
	
	.kawaii-avatar {
		flex-shrink: 0;
		width: 80px;
		height: 80px;
		border-radius: 50%;
		overflow: hidden;
		border: 2px solid var(--neon-pink);
		box-shadow: 0 0 8px var(--neon-pink);
	}
	
	.kawaii-avatar img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
	
	.message-bubble {
		position: relative;
		background: rgba(255, 105, 180, 0.1);
		padding: 1rem;
		border-radius: 0.5rem;
		border: 1px solid var(--neon-pink);
	}
	
	.message-bubble::before {
		content: '';
		position: absolute;
		left: -10px;
		top: 50%;
		transform: translateY(-50%);
		border-width: 10px 10px 10px 0;
		border-style: solid;
		border-color: transparent var(--neon-pink) transparent transparent;
	}
	
	.message-bubble p {
		font-family: var(--font-kawaii);
		font-size: 1.2rem;
	}
	
	@media (max-width: 576px) {
		.contact-item {
			flex-direction: column;
			text-align: center;
		}
		
		.contact-icon {
			margin-bottom: 0.5rem;
			width: auto;
		}
		
		.social-links {
			justify-content: center;
		}
		
		.kawaii-message {
			flex-direction: column;
			text-align: center;
		}
		
		.kawaii-avatar {
			margin: 0 auto;
		}
		
		.message-bubble::before {
			display: none;
		}
	}
</style> 